[pytest]
# The package under test lives at the repo root; make it importable without an
# install step so `pytest` works the same on the Mac and on officepi.
pythonpath = .
testpaths = tests
addopts = -q --strict-markers
